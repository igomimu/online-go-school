# 三村囲碁オンライン 修正タスク

## 2026-09-02 別ウィンドウを開いた元画面の碁盤を閉じる
- [x] 別ウィンドウ起動とホーム碁盤状態の実装箇所を特定する
- [x] 別ウィンドウが開いたら元画面をホームへ戻す
- [x] ポップアップがブロックされた場合は元画面を維持する
- [x] 関連テスト・Lint・本番ビルドを確認する

### Review
- 原因: 対局盤の「別ウィンドウ」ボタンが新しい窓を開くだけで、元画面の対局盤を閉じていなかった。両方が同じ対局を購読するため、着手音も二重に鳴っていた。
- 修正: `window.open` が成功した場合だけ既存のホーム復帰処理を呼び、元画面の対局盤をアンマウントする。ポップアップがブロックされた場合は元画面を残す。
- 検証: GameBoard単体41件、全体Vitest 84ファイル767件、ESLint、本番ビルドが成功。

## 2026-09-01 本格使用初回の修正
- [x] レッスン中の気づき5点を記録する
- [x] カメラ映像だけが停止した場合の再生復旧を追加する
- [x] 生徒リストのマイク・スピーカーを講師目線の意味へ統一する
- [x] 生徒別音声設定を講師の再読込・再接続後に復元する
- [x] 中断局へ「検討」と「再開」を別々に表示する
- [x] 中断後の新規対局が生徒画面で開く経路を修正する
- [x] 単体テスト・E2E・Lint・本番ビルドを確認する

### Review
- 映像: 本番のRealtimeKitで、映像要素が停止・待機・フォーカス復帰した時に再生を再試行し、トラックが生きたまま10秒間フレームが来ない場合はMediaStreamを付け直す。囲碁盤・教室接続は切らない。実授業での再確認は次回必要。
- 音声: マイクを「講師の声を生徒へ届ける」、スピーカーを「生徒の声を講師が聞く」に変更。生徒別状態を教室単位で端末へ保存し、講師の再読込・再接続時に再送する。
- 中断棋譜: 履歴に「検討」「再開」を併記。中断局は中央の進行中碁盤から外す。
- 新規対局: 生徒の自動オープンを描画中の状態変更からeffectへ移し、新規対局作成時はSupabase Realtimeに加えてRTCの確実な合図でも生徒に一覧再取得を促す。
- 検証: ESLint、Vitest 84ファイル765件、production buildが成功。中断→同じ生徒同士で新規対局→生徒2画面に新しい碁盤が自動表示されるE2Eを含む2件が成功。

## 2026-08-23 着手が相手へ届かない・考慮時間の発音
- [x] 双方向の着手保存・Realtime購読経路を確認する
- [x] games UPDATE時と定期照合でサーバー棋譜へ追いつく保険を追加する
- [x] 考慮時間の「入りました」を発話用に「はいりました」へ変更する
- [x] 双方向回帰テスト・全体検証・本番反映を確認する

### Review
- `submit_move` は着手INSERT後に対局行の `updated_at` も更新するため、そのUPDATE受信時に棋譜を再取得する。さらにRealtimeのINSERT・UPDATEを両方取り逃しても、3秒間隔でサーバー棋譜へ追いつく。
- サーバー棋譜との照合では確定済み行を正本にし、同じ手番号の仮着手を置換する。まだ送信中でサーバーに存在しない仮着手だけは画面から消さない。
- 発話文字列を「N回目の考慮時間にはいりました。残りN回です。」へ変更。Vitest 68 files / 640 tests、TypeScript、ESLint、production buildが成功。機能コミット `c13beb6` を本番へ反映し、`version.json` と配信JSの新しい発話文字列で確認した。

## 2026-08-23 新規対局より古い中断局が表示される問題
- [x] 新規作成から講師別ウィンドウの盤選択までを追跡する
- [x] 生徒の初手後にだけ新規局へ切り替わる原因を特定する
- [x] 新規対局のINSERT受信時点で新規局へ切り替える
- [x] 終局後に古い中断局へ自動復帰しないようにする
- [x] 回帰テスト・全体テスト・ビルド・本番反映を確認する
- [x] 作成INSERTを講師窓が取り逃す競合を、新規対局IDの直接引継ぎで解消する
- [x] 初手前表示の再現テスト・全体検証・再度の本番反映を確認する

### Review
- 原因は講師別ウィンドウの盤選択が、作成INSERTでは古い中断局を保持し、初手による盤面更新時だけ次盤選定を行っていたこと。さらに新規局が終局すると、一覧先頭の古い中断局へ自動フォールバックしていた。
- 新しく追加された `playing/scoring` の対局IDを初手前に選択し、自動表示対象を進行中・整地中だけに限定。中断局は一覧に残すが、明示的に選択した場合だけ表示する。
- 講師窓を「中断局のみ→新規局INSERT→新規局終局」の順で再描画する画面テストを追加。Vitest 65 files / 633 tests、TypeScript、ESLint、production buildが成功。機能コミット `df3f92e` を本番へ反映し、`version.json` と配信JSの新しい空状態表示で確認した。
- 再調査で、作成前に開いた講師窓の初回fetchとRealtime購読開始の間へINSERTが入る競合を確認。作成APIの戻り値から新規ID付きURLへ講師窓を切り替え、確定IDを直接表示するようにした。INSERT未受信状態の画面テストを含むVitest 66 files / 636 tests、TypeScript、ESLint、production buildが成功。機能コミット `361a49f` の本番反映を `version.json` と配信JSの `teacherGameId` で確認した。

## 2026-08-15 生徒一覧から対局を中断・再開
- [x] 生徒一覧の対局状態判定と既存の中断・再開処理を確認する
- [x] 「対局」「詳細」列を1つの「操作」列へ整理する
- [x] 対局中・整地中へ「中断」、中断・時間切れへ「再開」を表示する
- [x] 状態別の操作テスト、全体テスト、ビルド、Lintを確認する
- [x] mainへpushし、本番反映を確認する

### Review
- 生徒一覧の「対局」「詳細」を削除し、1列の「操作」へ集約。盤を開く操作は中央の碁盤クリックへ一本化した。
- 対局中・整地中は「中断」、中断局・時間切れ局は「再開」を表示。通常終局や未対局の生徒には操作を出さない。
- 古い中断局が残っていても現在進行中の対局を優先し、誤って過去局を再開しないようにした。生徒が一時切断中でも対局操作は可能。
- 検証: 状態別6件を含む関連46件、既知のSupabase環境依存ファイルを除く全体59ファイル562件、TypeScript/Vite本番ビルド、ESLint、差分チェックが成功。
- 本番: commit `2dcff9e9d573669adad2cb91a4b5f75942a0901b` を `online.mimura15.jp` へ反映し、`version.json` で一致を確認。

## 2026-08-15 授業中の棋力表示切替
- [x] 授業中画面と教室設定の保存経路を確認する
- [x] 講師画面の教室タイトル行に「段級／ランク」切替を追加する
- [x] 教室設定だけを安全に保存し、画面へ即時反映する
- [x] 秒読みの文字表示を、音声と同じ経過秒のカウントアップへ統一する
- [x] 関連テスト・ビルド・Lintを確認する
- [x] mainへpushし、本番反映を確認する

### Review
- 授業中の教室タイトル右側へ「棋力表示　段級｜ランク」を常設。押した値をその場で生徒一覧・碁盤見出しへ反映し、教室単位で保存する。
- 表示切替専用の更新処理を追加し、名簿全体の保存や生徒の所属・並び順の書換えは行わない。
- 秒読みの内部時計は残り秒のまま維持し、文字表示だけを音声と同じ経過秒（0→設定秒数）へ変換。時間切れ判定には影響しない。
- 検証: 関連39件、既知のSupabase環境依存ファイルを除く全体59ファイル559件、TypeScript/Vite本番ビルド、ESLint、差分チェックが成功。
- 本番反映: 機能コミット `afae40f` と修正コミット `c6c1c40` をmainへpush。Vercel ProductionがREADYとなり、`online.mimura15.jp/version.json` で配信を確認した。

## 2026-08-15 講師画面の参加者映像を上部へ移動
- [x] 最新の `origin/main` を基準に、既存の映像配置と見切れの原因を確認する
- [x] 参加者映像を教室タイトル直上へ横一列で移動する
- [x] 映像を見やすい比率・大きさにし、人数が多い時は横スクロールできるようにする
- [x] 映像の個別拡大操作を追加する
- [x] ヘッダーの音声・映像設定を画面内に収まる固定パネルへ変更する
- [x] 狭い画面でも設定パネル全体を操作できることを確認する
- [x] 検討盤のホイール手順移動で碁盤サイズが変わらないようにする
- [x] 対局中の着手・手番切替で碁盤サイズが変わらないようにする
- [x] 通常終局を講師だけに通知音で知らせる（時間切れは既存の専用音を維持）
- [x] AI形勢判断を黒基準へ統一し、棋譜のコミ・初期配置を解析へ反映する
- [x] 秒読みなし選択時、未入力の持ち時間を30分へ初期化する
- [x] 参加者映像列を上下ドラッグで拡大・縮小できるようにする
- [x] 講師が対局を「中断」し、棋譜履歴または対局画面から後で再開できるようにする
- [x] 一時切断・ページ再読込後も、直前にONだったマイクとカメラを自動復元する
- [x] 時間切れ対局の再開ボタンを多面打ち一覧にも表示する
- [x] テスト・ビルド・画面表示を確認する
- [x] 変更を安全な作業ブランチへコミットする
- [x] テスト終了後にpushし、本番反映を確認する

### Review
- 原因: 固定幅280pxの右サイドバーに、1枚120pxの映像を中央揃えで3枚並べていたため、必要幅約400pxが収まらず左右が見切れていた。
- 修正: 講師画面の参加者映像を教室タイトル直上へ移動。16:9・全体表示の大きいタイルを左起点で並べ、人数が多い時は映像列だけ横スクロールできるようにした。各映像は全画面表示も可能。
- 設定パネル: ヘッダー内の絶対配置をやめ、画面右上の固定パネルとして `body` 直下に表示。ヘッダー高を変えず、画面高が低い時だけパネル内部をスクロールする。
- 検討盤: 手ごとにコメント・変化一覧が出入りして碁盤の可変高さを奪っていたため、PCの手順・注釈・コメント領域を一定高にした。コメント有無と分岐有無が違う3手をホイール移動し、SVGの縦横寸法が全手で同一であることを確認。
- 対局盤: 手番ごとにパス・投了・待ったボタンが出入りして碁盤の可変高さを奪っていたため、対局中の操作欄を常に36px確保。狭い画面では操作欄だけ横スクロールし、着手のたびに碁盤寸法が再計算されないようにした。
- 終局音: 旧LiveKitメッセージだけでなく、現在のSupabase Realtimeのfinished更新を直接検知。講師だけ・同じ終局につき1回チャイムを鳴らし、時間切れは既存の専用警告音と重複させない。画面を開いた時点で既に終局済みの対局は鳴らさない。
- AI: KataGoの「次に打つ側」基準の数値を黒基準へ正規化していなかったため、白番局面で勝率・目数差が逆転していた。棋譜のコミを常に6目半にしていた点と、置き石を送っていなかった点も修正。
- 対局時計: 秒読みなしを選び、持ち時間が未入力（0分）なら30分を自動入力。既に15分などを入力済みの場合は上書きしない。
- カメラ列: 個別全画面表示に加え、列の下の「映像の高さを調整」を上下ドラッグ（または上下矢印）して96〜360pxで変更可能にした。変更した高さは端末に保存し、映像は16:9・全体表示を維持する。
- 打ち掛け: 対局中・整地中の講師画面に「中断」を追加。中断時はサーバー側でその時点の棋譜を履歴へ保存し、時計を停止する。中断後は対局画面と生徒別の棋譜履歴に「再開」を表示する。
- 接続復帰: 対局状態はSupabaseから戻っていた一方、マイク・カメラの利用意図はReact stateだけで失われていた。役割別に端末へ保存し、初回接続・LiveKit自動再接続・手動回線復旧で自動復元する。講師によるマイク禁止と明示的な退出はOFFを維持する。
- 時間切れ再開: 講師ホーム・通知・棋譜履歴・対局盤に加え、多面打ち別ウィンドウの一覧にも「再開」を接続。「中断」と誤表示していた状態ラベルも「時間切れ」に修正した。
- 検証: 全体Vitest 58ファイル556件（Supabase環境変数が必要な既存useGameManagerファイルのみ除外）、TypeScript/Vite本番ビルド、ESLint成功。Chromium 1522×934・800×600で、映像の欠け、横送り、設定パネルの上下端、開閉前後のヘッダー高を確認。対局盤は自分の手番・相手の手番の両状態で操作欄が同じ固定高を維持し、中断・再開は講師だけ操作できるテストを追加。メディアは保存・役割分離・再接続復元・片方失敗時の継続をテストした。
- 本番反映: 機能コミット `9e1619d` をmainへpush。Vercel Production `dpl_AAhdYKTasU3tNg9jv214yn5bYkxL` がREADYとなり、`online.mimura15.jp/version.json` で同コミットの配信を確認した。

## 2026-08-11 生徒自身の棋譜履歴と再開時計の本番反映
- [x] 生徒は自分の棋譜だけ取得できるRLSへ変更する
- [x] 生徒ロビーに「自分の棋譜履歴」を追加する
- [x] 生徒が棋譜を開いても他参加者へ検討開始を配信しない
- [x] 時間切れ対局の再開直後から秒読みを開始する
- [x] テスト・ビルド・本番反映・本番確認を行う

### Review
- 生徒ロビーへ「自分の棋譜履歴」を追加。ログイン中の生徒IDでSupabaseを絞り込み、選択した棋譜を本人の検討盤で開ける。
- 生徒が履歴を開いた場合はLiveKitへ `REVIEW_START` を送らず、他の参加者の画面を切り替えない。先生が開いた場合の共有動作は維持。
- `go_school_games` のRLSを、先生は全件、生徒は黒番または白番が自分のID（bare / `sid:`）の棋譜だけに制限。黒番・白番列へ索引を追加。本番検証は生徒1001で14件表示・他人生徒棋譜0件、先生は187件表示。
- 時間切れ対局の再開では、復元した時計の `lastTickTime` に再開時刻を設定し、再開直後から手番側の秒読みを開始。
- 検証: LEGIONでVitest 54ファイル524件、ビルド、ESLintが成功。Edge Function `manage_game_action` は `6ae0f83`、Vercel本番は `cc607d2`。`/check.html` と本番JSの「自分の棋譜履歴」を確認。

## 今後の検討課題
- [ ] 生徒が個別にLEGIONのKataGoを使って自由検討できる機能
  - 生徒ブラウザからLEGIONへ直接接続せず、オンライン側APIと解析待ち行列を経由する
  - 授業中は講師解析を最優先にし、生徒解析は講師が許可した場合だけ有効にする
  - 生徒ごとの同時解析数、探索数、キャンセル、キャッシュ共有、レート制限でGPU負荷を制御する

## 2026-08-02 講師のAI予想手順を生徒へ同期
- [x] AI候補手のホバー順位をLiveKit同期データへ追加する
- [x] 講師が候補へマウスを置いた時、生徒盤へ同じPVを表示する
- [x] 生徒操作は標準OFFとし、講師が許可した時だけ候補ホバーを有効にする
- [x] 単体テスト・本番実通信・デプロイを確認する

### Review
- 生徒のAI操作は標準OFF。講師が候補手へマウスを置いた時だけ、同じPV手順を生徒盤へ同期表示する。
- 講師が「生徒の候補手操作を許可」をONにした場合だけ、生徒自身の候補ホバーを有効にする。講師が手順を示している間は講師操作を優先する。
- AI解析は引き続き講師側だけがLEGIONのKataGoへ要求し、生徒側は同期結果の表示だけを行う。
- 検証: Vitest 47ファイル・467件すべて成功。本番 `b2abbfb` の実LiveKit通信で、講師ホバー時2手、標準ロック時の生徒操作0手、許可時の生徒操作2手を確認。

## 2026-08-02 AI探索数をPocket KataGoと統一
- [x] Pocket KataGoとオンラインの探索数設定を比較する
- [x] 標準値を1000から3000 visitsへ変更する
- [x] 既存ブラウザに残る旧100〜1000設定を一度だけ3000へ移行する
- [x] テスト・本番反映・実APIログを確認する

### Review
- AIエンジンと接続先はPocket KataGoと共通だが、オンライン側の端末設定が100〜1000 visitsのまま残っていた。候補一覧の80/100は総探索数ではなく各候補へ配分された探索数で、総探索100ではK単位にならない。
- 標準をPocket KataGoと同じ3000 visitsへ変更。設定保存形式をv2にし、旧形式は保存値が100/500/1000等でも初回だけ3000へ移行する。移行後に利用者が軽量設定を選び直した場合は保持する。
- 本番KataGoへ3000 visitsを送信し、HTTP 200・1.89秒、上位候補2967 visits（約3.0K）、PV 8手を確認した。
- Vitest 47ファイル465件すべて成功、TypeScript/Vite build成功、対象Lintエラー0。修正コミット `4d27e28` を本番反映し、`online.mimura15.jp/version.json` で配信を確認した。

## 2026-08-01 Pocket KataGo型AI表示・生徒同期
- [x] Pocket KataGoの候補手・PV表示と二段階解析の操作仕様を移植する
- [x] PCを碁盤50%・AI/授業情報50%の2カラムにする
- [x] AIのON/OFF・解析結果・候補手・PVを先生から生徒へ同期する
- [x] OFF時の中断・表示消去と、ON時の自動解析を保証する
- [x] 単体テスト・ビルド・PC画面・先生/生徒同期を検証する
- [x] コミット・push・本番反映後に実ログを確認する

### Review
- Pocket KataGo実装を基準に、局面移動後50msで10 visitsの速報を出し、その後に設定visitsまで読む二段階解析を移植した。OFF時は進行中リクエストをabortし、候補・PV・スピナーを即時非表示にする。
- 碁盤上に1位=水色、2位=緑、3位以降=黄の候補石を表示し、勝率・visits・目数差を併記。候補石または候補一覧へのホバーで、PVを黒白の番号石として表示する。
- 先生端末だけがKataGoへ接続し、AIのON/OFF・解析中・エラー・上位5手・PVを `AI_ANALYSIS_UPDATE` で生徒へ配信する。生徒側の同期盤面が検討モードを授業モードへ上書きしていた既存原因も修正した。
- PC実測: 碁盤694px、AI/チャット698px。LiveKitを使った本番生徒画面でON、候補手、盤上候補、PV、OFF後の候補0件・スピナー0件・コンソールエラー0件を確認した。
- 検証: Vitest 46ファイル459件すべて成功、追加修正の関連18件成功、TypeScript/Vite build成功。KataGo本番APIは10/1000 visitsともHTTP 200で候補手とPVを返した。
- 本番反映: 機能コミット `ae67c76`、生徒表示優先順位修正 `af20835`。`online.mimura15.jp/version.json` で `af20835` 配信を確認した。

## 2026-08-01 AI分析の無限ローディング修正
- [x] 本番KataGo APIの疎通とVercel実行ログを確認する
- [x] 同一局面の解析リクエストが連続送信される状態を特定する
- [x] 同一局面の重複送信を防ぐ安定キーを実装する
- [x] 接続停止時のタイムアウトと日本語エラー表示を実装する
- [x] 単体テスト・全体テスト・ビルドを確認する
- [x] 修正をコミット・pushし、本番反映を記録する

### Review
- 原因: `useAiAnalysis` が手順配列などの参照をeffect依存値にしていたため、同じ局面でも再レンダーのたびに前回を中断して再送していた。本番VercelログでKataGo応答はHTTP 200なのに、約0.5秒間隔で同じAPIが連続実行されることを確認した。
- 修正: 局面・手順・盤サイズ・コミ・visitsから安定した解析キーを作り、同じ内容はキャッシュ/進行中リクエストを再利用する。旧直結方式の「サーバーURL」設定も削除した。
- 障害時: ブラウザ側20秒、Vercel→KataGo側15秒のタイムアウトを追加し、無限待機せず日本語エラーを表示する。
- テスト: 新規3件（重複防止・局面変更・タイムアウト）成功。全体は452/453件成功し、既知の並列負荷タイムアウト1件は単独再実行4/4件成功。Lint成功、`npm run build` 成功。
- 本番反映: 修正コミット `32da4cb`。GitHub build・Vercel deploy成功。本番KataGo APIは9路・100 visitsでHTTP 200、1.17秒で解析結果を返し、配信JSに20秒タイムアウト処理が含まれることを確認した。

## 2026-08-01 入室画面の中央配置修正
- [x] `origin/main` の最新版を取得し、隔離したクリーン作業ツリーで再現する
- [x] 入室画面が左へズレる原因をDOMの実測とスクリーンショットで特定する
- [x] 生徒・先生の入室画面をPC・スマホ幅で中央配置にする
- [x] テスト・ビルド・スクリーンショットで回帰確認する
- [x] 修正をコミット・pushし、反映結果を記録する

### Review
- 原因: `body` が横方向のFlexコンテナなのに `#root` に幅指定がなく、入室画面では `#root` が内容幅（約326px）まで縮んで左端に配置されていた。
- 修正: `#root` に `width: 100%` と `min-height: 100vh` を指定し、アプリのルート領域を常にビューポート全体へ広げた。
- 画面検証: 生徒/先生ログインを1280x800・390x844で確認し、カード中心とビューポート中心の差はいずれも0px。コンソールエラーなし。
- 自動検証: Vitest 43ファイル・450件すべて成功、`npm run build` 成功。
- 反映: 修正コミット `81ef181` を `origin/main` へpush済み。

## 2026-07-19 未コミット変更の整理
- [x] 未コミット差分を機能別に確認する
- [x] Deno検証で自動生成された不要なlock差分を除外する
- [x] 全体テスト・Denoテスト・ビルドを再実行する
- [x] 整合した変更一式をコミットしてmainへpushする
- [x] push後のリポジトリ状態を確認する

### Review
- 実施結果: 置石、ID厳格化、生徒編集、進行中対局復旧、講師名表示、対局作成保護、E2E清掃、碁盤描画を現在の整合した状態で1コミットに集約。Deno検証が生成しただけの `deno.lock` 差分は除外した。
- 検証結果: Vitest 38ファイル394件、Deno 6 suites/16 steps、ESLint（エラー0、既存警告6件）、`npm run build` が成功した。
- 残課題: なし。ビルド時の既存chunk-size警告とBrowserslist更新警告は今回の変更外。

## タスク一覧
- [x] 14. テスト由来の未終了対局を清掃し、再発を防止
  - [x] OSプロセス・LiveKitルーム・本番DBの残留状態を確認する
  - [x] 明確なE2E用教室IDの対局・教室データだけを削除する
  - [x] セキュリティE2Eに終了時清掃を追加する
  - [x] 清掃後の本番状態とテストを確認する
- [x] 13. 1001/1002相手の多面対局で1局だけになる問題を修正
  - [x] 手動対局作成ダイアログが開始後に閉じるようにする
  - [x] 別生徒から開き直した時に `initialBlackPlayer` を反映する
  - [x] 作成中の二重押しを禁止する
  - [x] Edge Functionで同じ2人の進行中対局の二重作成を拒否する
  - [x] テスト・Edgeデプロイ・Vercelデプロイで検証する
- [x] 12. 4桁IDのユーザー認識を部分一致から完全一致に修正
  - [x] `/api/token` の LiveKit 入室認可で `identity.includes(student_id)` を廃止する
  - [x] `sid:` 正規化後の完全一致ヘルパーを追加する
  - [x] 1002 と 10020 / x1002 を同一人物扱いしないテストを追加する
  - [x] テスト・Vercel build・本番デプロイで検証する
- [x] 11. 教師ホームの進行中碁盤と別ウィンドウ対局盤の表示を修正
  - [x] 教師ホームの進行中碁盤一覧へライブ着手から復元した実盤面を反映する
  - [x] 別ウィンドウの対局盤でも名簿を読み込み、`sid:<id>` / `teacher` を表示名へ解決する
  - [x] 関連テスト・全体テスト・ビルド・本番デプロイで検証する
- [x] 10. 講師側の `teacher` 表示を講師表示名にする
  - [x] `teacher` は内部IDとして残し、画面表示では講師名へ変換する
  - [x] 講師ログイン画面で講師表示名を変更・保存できるようにする
  - [x] 既定値を `三村九段` にする
  - [x] テスト・ビルド・本番デプロイで検証する
- [x] 9. 棋譜保存・対局中判定で別ID同名生徒が混ざる問題を修正
  - [x] 生徒別棋譜履歴はIDがある場合に名前検索を混ぜない
  - [x] 講師生徒表・サムネイル・接続中判定から名前照合/部分一致を除去する
  - [x] 講師画面に、通常一覧に出ていない進行中対局の検出・状態解除導線を追加する
  - [x] Edge Function経由で教室IDに依存せず生徒IDの進行中対局を検出できるようにする
  - [x] 全体テスト・ビルド・本番デプロイで検証する
- [x] 8. 講師の2局同時対局で片方の生徒が対局を開けない/着手できない問題を修正
  - [x] `sid:<id>` と素の `<id>` が混在しても同一対局者として判定する
  - [x] 生徒ロビー・講師生徒表・講師サムネイル・多面打ち手番判定を同じidentity比較に統一する
  - [x] 講師を含む複数対局の一括作成でも講師対局ウィンドウを開く
  - [x] 関連テスト/全体テスト/ビルドで検証する
- [ ] 7. 講師が教室内で生徒IDを含む全生徒情報を編集できるようにする
  - [x] リモート `origin/main` の更新を確認してローカル `main` を追従
  - [x] 講師ダッシュボードの生徒編集を生徒ID・姓名・棋力・内部R・種別・生年月日・学年・所在地対応に拡張
  - [x] 生徒ID変更時に教室所属・表示順を保持し、重複IDを拒否する
  - [x] テスト/ビルドで検証する
- [x] 6. 置き石配置を三村囲碁オンライン仕様に変更
  - [x] 2子は右上と左下に置く
  - [x] 3子は左上を空け、右下を3子目にする
  - [x] ローカル対局生成とSGF出力の両方を同じ配置にする
  - [x] 関連テストで検証する
- [x] 5. 対局・検討・問題・棋譜を開いた時にホーム画面項目を隠して碁盤を大きく表示
  - [x] `game` / `review` / `problem` 中はヘッダー、動画タイル、接続/音声パネルを非表示にする
  - [x] 詰碁画面も対局・検討と同じ全画面オーバーレイにする
  - [x] `GoBoard` の高さ制限を画面ごとに上書き可能にする
  - [x] 対局・検討/棋譜・問題の全画面に「閉じてホーム」ボタンを付ける
  - [x] ビルド/テストで検証する
- [x] 4. 終局後に棋譜履歴へ保存されない問題の修正
  - [x] `manage_game_action` の `finish` で `go_school_games` にSGFを保存する
  - [x] 「中断」は再開対象なので履歴保存から除外する
  - [x] 生徒別履歴検索で `sid:<uuid>` 形式も検索する
  - [x] SGF生成・履歴検索のテストを追加する
  - [x] ビルド/テストで検証する
- [x] 1. 対局中のままログアウトした時に確実に「中断」扱いにする修正
  - [x] `src/App.tsx` の `handleDisconnect` で `finishGame` を `await` してから `supabaseSignOut` などを実行するように順序修正
- [x] 2. 着手表示の遅延（数秒）の修正
  - [x] `src/types/game.ts` に `GameMovePayload` や `GAME_PASS` 用の `moveNumber` を追加
  - [x] `src/App.tsx` の `onMessage` ハンドラで、対局メッセージ（`GAME_MOVE`, `GAME_PASS`, `GAME_RESIGN`）を受信した時にカスタムイベント `'live-game-message'` を発信するように修正
  - [x] `src/components/GameBoard.tsx` で `classroom` インスタンスを props として受け取り、`useLiveGame` フックに渡すように修正
  - [x] `src/App.tsx` の `GameBoard` レンダリング箇所で `classroom={classroomRef.current}` を渡すように修正
  - [x] `src/hooks/useLiveGame.ts` で `classroom` 引数を追加し、楽観的更新と LiveKit メッセージの送信・受信を実装。また `onMoveInsert` 時の重複排除・差し替えロジックを実装
- [x] 3. 動作検証
  - [x] ローカルビルド確認 (TypeScript typecheck 正常終了)
  - [x] 本番環境（Vercel）へのデプロイ

## 2026-08-22: 検討対象切替・黒白入替

- [x] 最新の `origin/main` を隔離worktreeで確認する
- [x] ホーム画面で検討対象を開始前に選べる既存導線を確認する
- [x] 検討中に対象外へ切り替えた生徒を即時退出させる
- [x] 検討中に対象へ戻した生徒へ開始データと現在盤を送る
- [x] 対局者選択の間に黒白入替ボタンを追加し、手合割を維持する
- [x] 関連テスト・全体テスト・ビルド・画面表示を検証する

## レビュー結果
- 2026-08-22: 検討開始前・検討中の参加生徒オン/オフを連動。途中で外した生徒は対局中なら対局盤、それ以外はホームへ戻し、途中で戻した生徒には開始データ・現在盤・着手権限を順に即時配信する。対局作成画面には黒番・白番選択の間に入替ボタンを追加し、選択済みの手合割を維持する。
- 検証: `node --env-file=<既存env> node_modules/vitest/vitest.mjs run --reporter=dot` 61 files / 603 tests passed、対象テスト28件 passed、ESLint成功、`npm run build`成功。共有DBを使う `sharing-target.spec.ts` は、手元の旧service-roleキーがSupabase側で無効化済み（`Legacy API keys are disabled`）のためseed前に停止。途中参加・退出の差分判定とホーム/対局作成UIは単体テストで固定した。

## 2026-08-23: 複数教室所属・子ども向け再入室

- [x] 生徒と教室の多対多所属を追加し、既存の単一所属を移行する
- [x] 生徒ログイン時に参加先教室への所属をサーバー側で検証する
- [x] リンク指定なしで複数所属の場合は教室名から選べるようにする
- [x] 誤った教室IDを受け入れず、所属教室へ安全に案内する
- [x] ウィンドウを閉じても中断対局へ簡単に復帰できるようにする
- [x] 関連テスト・全体テスト・ビルドを検証する

## レビュー結果
- 生徒プロフィールと教室所属を多対多テーブルへ分離。既存27件の所属を移行し、鈴木榛人（1016）は正しい教室 `CLS20160919347` に所属していることを本番DBで確認した。
- 生徒コードだけで本人を特定し、所属が1教室なら正しい教室へ自動補正、複数なら教室名ボタンから選択する。教室IDの手入力欄は廃止した。
- 中断対局IDをウィンドウ終了で消える保存先から端末内の永続保存へ移し、再開成功後にだけ削除するよう変更した。
- 本番DB migrationと `validate_student_session` / `list_classroom_roster` / `list_classroom_students` を反映済み。誤った教室IDで1016を認証し、正しい教室へ補正されることを実測した。
- 検証: Vitest 64 files / 615 tests passed、Deno helper test 3 steps passed、Edge Function 3本の `deno check` 成功、ESLint成功、production build成功。

## 2026-08-23: 開始済み対局の取消・中断局の残留解消

- [x] 先生ホームの生徒行から進行中対局を取り消せるようにする
- [x] 中断局にも「再開」と「取消」を並べ、不要な局を解除できるようにする
- [x] 取消は講師専用とし、途中保存された中断棋譜も履歴から削除する
- [x] Realtime不調時も取消後の一覧を再取得して表示を消す
- [x] 関連テスト・全体テスト・本番反映を検証する

## レビュー結果
- 先生ホームの「対局操作」列に取消を追加。進行中は「中断／取消」、中断中は「再開／取消」を表示する。
- 取消時は確認後に対局を終了扱いにし、中断時に保存済みの同一ID棋譜も削除する。生徒セッションからの取消はEdge Functionで403拒否する。
- 取消成功後に対局一覧を明示的に再取得するため、Realtimeイベントを受け損ねてもホーム表示から消える。
- 検証: StudentTable 9/9、Vitest 64 files / 617 tests、ESLint、production build、`deno check manage_game_action` がすべて成功。本番で検証用中断局と棋譜を作成し、取消後に `status=finished / result=取消 / historyCount=0` を確認して検証データを削除した。
- 本番反映: Supabase `manage_game_action` と Vercel production `eae3901`。

## 2026-08-23: 棋譜履歴の個別削除・中断局を残した新規対局

- [x] 講師の棋譜履歴リスト各行に削除ボタンを追加する
- [x] 削除は講師専用Edge Function経由にし、生徒から拒否する
- [x] 中断棋譜の削除時は対応する中断対局もホームから解除する
- [x] 中断局が残っていても同じ対局者で新規対局を作成できるようにする
- [x] 関連テスト・全体テスト・本番反映を検証する

## レビュー結果
- 講師が生徒行の「履歴」を開くと、すべての棋譜に個別の「削除」ボタンを表示する。削除中は対象ボタンだけを無効化し、成功後は一覧から即時除去する。
- 中断棋譜の削除では同じIDの中断対局を `finished / 取消` にしてから保存棋譜を削除し、ホームの再開待ちも同時に解消する。進行中の対局に対応する保存棋譜だけを削除した場合は対局を終了させない。
- 新規対局の重複判定は `playing / scoring` のみに変更し、過去の `interrupted` が残っていても同じ対局者で新しい局を開始できる。
- 検証: Deno lifecycle test 2 steps、`deno check manage_game_action`、ESLint、production build、Vitest 64 files / 617 testsが成功。本番で「中断局あり→同一対局者の新規局作成」「生徒削除403」「講師削除→中断局取消・履歴0件」を実測し、検証データを削除した。
- 本番反映: Supabase `manage_game_action`、Vercel production `2683c41`。

## 2026-08-23: 生徒行からの対局作成

- [x] ホーム画面の生徒行「対局操作」に「作成」ボタンを追加する
- [x] 押した生徒を対局者として選択済みの状態で対局作成画面を開く
- [x] 進行中は非表示、中断局が残る接続中生徒には表示する
- [x] 関連テスト・全体テスト・ビルド・本番反映を検証する

## レビュー結果
- 接続中で進行中の対局がない生徒には、「検討」の右隣にある「対局操作」列の先頭へ「作成」を表示する。押した生徒は対局作成画面の黒番に選択済みとなり、白番は講師になる。
- 中断局・時間切れ局が残る生徒は「作成」と「再開」を選べる。進行中または未接続の生徒には「作成」を表示しない。
- 対象テスト28件、Vitest 64 files / 620 tests、ESLint、production buildが成功。アプリ内ブラウザはIAB接続先が検出されず、実画面操作の代わりにDOMテストで状態別表示と選択identityの引継ぎを確認した。

## 2026-08-23: 対局作成画面の再構成

- [x] 生徒表を「対局」列・「新規」ボタンへ短縮する
- [x] 7種類の碁盤サイズと全サイズの置き石配置に対応する
- [x] 講師対生徒・生徒同士を黒白ラジオと相手選択で設定できるようにする
- [x] 置き石・コミをドロップダウン化し、コミの「その他」入力を追加する
- [x] 時間制限とNHK杯方式を指定範囲のドロップダウンで設定できるようにする
- [x] 関連テスト・全体テスト・ビルド・本番反映を検証する

## レビュー結果
- 対局作成画面を指定順に再構成し、通常時計と「1手30秒＋60秒考慮時間」のNHK杯時計を別モードにした。
- NHK杯の音声は、最初の30秒、継続する60秒、最後の60秒を分け、最終回は黒・白を付けて時間切れを案内する。
- Vitest 64 files / 626 tests、Deno共有処理5 suites / 16 steps、TypeScript、ESLint、production buildが成功。Web本番でコミット `a107af9` と新しい対局作成UIの配信を確認し、Supabase `manage_game_action` v52を本番反映した。
- 2026-07-18: 本番DBに残っていた `test-class-*` / `wiring-*` / `debugfull-*` / `verify-*` / `single-*` / `reconnect-*` の未終了E2E対局59件と、教室レコードがなくE2E専用生徒1010だけが参加していた孤立 `CLS001` 中断局1件、対応する残存テスト教室2件を削除。実教室 `CLS20160919347` は清掃前から未終了0件で、1001/1002の直近局はいずれも正常終局済みだったため変更していない。
- 再発防止: `e2e/security.spec.ts` に `afterAll` 清掃を追加し、`teardownSupabaseRoster` が対局削除・生徒紐付け解除のエラーを黙殺しないよう修正。Playwrightの古い `.bin` リンクもnpm版へ戻した。
- 検証: OS defunct 0件、重複開発サーバー0件、LiveKitルーム0件、自動テスト由来の未終了対局0件・テスト教室0件。`BASE_URL=https://online.mimura15.jp npx playwright test e2e/security.spec.ts --project=chromium --reporter=line` 3/3 passed、実行後も残骸0件、`npm run build` 成功。

## 2026-08-23: 生徒区分名の編集

- [x] 全教室共通の生徒区分マスターをSupabaseへ追加する
- [x] 教室設定から区分名の追加・変更・削除・並べ替えをできるようにする
- [x] 区分名の変更を、その区分が設定済みの生徒へ同時に反映する
- [x] 生徒の追加・編集画面を共通区分マスターへ切り替える
- [x] 関連テスト・全体テスト・ビルド・本番反映を検証する

## レビュー結果
- 生徒区分を全教室共通のマスターとして追加。教室設定から追加・名称変更・並べ替え・削除ができる。使用中の区分は削除を止め、名称変更時は該当生徒のプロフィールも同じDBトランザクションで更新する。
- 生徒管理ページと教室ホームの生徒編集ダイアログは、固定配列ではなく共通マスターを参照する。マスター外の旧区分も選択肢から消さず移行できる。
- 検証: 対象11テスト、Vitest 69 files / 644 tests、ESLint、production buildが成功。本番DBで初期区分12件、RLS、名称変更RPC、migration履歴を確認し、RPCのトランザクション試験もロールバック付きで成功した。アプリ内ブラウザは接続先が検出されなかったため、画面操作はDOMテストで名称変更・使用中削除の防止まで確認した。
- 追加修正: 全件入れ替えのDELETEへ `WHERE true` を明示し、Supabaseの「DELETE requires a WHERE clause」安全検査に対応。本番関数定義と修正migration履歴を確認し、同じRPCのロールバック試験も成功した。
- 追加修正: 使用中区分の削除も保存エラーにせず、影響人数を確認後、該当生徒を「未設定」へ移して削除する。本番DBで8名が使用中の区分を削除し、区分0件・旧割当0件になることを確認して全変更をロールバックした。
- 2026-07-18: 1001/1002をログインさせて講師が2名相手に対局を始めたが1局だけになる問題を調査。実教室 `CLS20160919347` の直近DBでは、作成された対局は `sid:1002 vs teacher` が2回で、`sid:1001 vs teacher` は新規作成されていなかった。原因候補として、手動対局作成ダイアログが開始後に閉じず、別生徒から開き直しても内部stateが最初の `initialBlackPlayer` のまま残る挙動を修正。開始後は閉じ、作成中は二重押し不可にした。さらに `manage_game_action` create で同一教室・同じ2人の進行中対局を409で拒否する保険を追加。
- 検証: 関連Vitest 22/22 passed、Deno identity shared test 4 suites/12 steps passed、`deno check manage_game_action` 成功、`npm run build` 成功、`npx vitest run --testTimeout=10000` 392/392 passed。本番Edgeで同じ `sid:1002 vs teacher` の2回作成を実測し、1回目200・2回目409を確認後、検証用対局を強制終局で清掃。Supabase `manage_game_action` と Vercel production `dpl_7PuABgJGiGe7LxHveEjbRVtCcvrK` を本番反映。実教室の進行中対局は最終確認で0件。
- 2026-07-18: 教師ホームの進行中碁盤一覧が `liveRowToSession` の空盤プレースホルダを表示していたため、`useLiveBoards` のライブ着手スナップショットを `GameSession` へ合成して実盤面・手数・手番が出るようにした。別ウィンドウ対局盤は `?mode=game` でも名簿を再読込し、対局者欄を `sid:<id>` や `teacher` ではなく生徒名・講師表示名で出すようにした。
- 2026-07-18: 4桁IDのユーザー認識で、Vercel `/api/token` が `identity.includes(student_id)` による部分一致認可をしていたため、`sid:` を剥がした完全一致に変更。`sid:1002` は `1002` と一致するが、`sid:10020` / `sid:x1002` は一致しないテストを追加した。DB実データ確認では1002=清水 菜奈子、1003=志賀 愛美として名簿・保存棋譜とも `sid:1002` / `sid:1003` に分離済み。
- 検証: 関連Vitest 16/16 passed、Deno identity shared test 3 suites/10 steps passed、`npx vitest run --testTimeout=10000` 391/391 passed、`npm run build` 成功、`npx vercel build --prod --yes` 成功。Vercel production deploy `dpl_3UN5mkRFteB4HBKuhgRtXgu5dz3D` 完了、`https://online.mimura15.jp/api/token` がモジュールエラーなく403応答することと、本番JSに `講師表示名` / `go-school-teacher-display-name` / `講師一覧に出ていない対局` / `状態解除` が含まれることを確認。
- 2026-07-18: 講師の保存用identity `teacher` が画面に出ていたため、表示名へ変換する共通処理を追加。講師ログイン画面に「講師表示名」欄を追加し、この端末のブラウザに保存できるようにした。未設定時の既定値は `三村九段`。LiveKit入室時の表示名にも保存値を使い、内部identityは常に `teacher` のまま維持する。
- 検証: 関連Vitest 40/40 passed、`npm run test` 385/385 passed、`npm run build` 成功。Vercel production deploy `dpl_Fmb2LdB6CktSQ7nzsVKd8tSFoZNw` 完了、`https://online.mimura15.jp` 配信JSに `講師表示名` / `go-school-teacher-display-name` / `三村九段` が含まれることを確認。
- 2026-07-18: 棋譜保存まわりの異常として、1002/1003のような別ID同名生徒が同じ生徒として扱われる経路を修正。`loadSavedGamesForStudent` はID指定時に名前検索を混ぜず、`studentIdentityCandidates` / `findStudentByIdentity` / `getDisplayName` も `sid:` 生徒identityではID・ログインコードのみ照合するようにした。講師生徒表・サムネイル・接続中判定の `includes` / 名前照合も除去。
- 2026-07-18: 生徒側だけに残り、講師側通常一覧に出ない進行中対局を解除できるよう、講師画面に「講師一覧に出ていない対局」警告欄と「状態解除」ボタンを追加。`manage_game_action` に講師専用 `list_active_for_players` を追加し、RLSや教室ID絞り込みで通常一覧に出ない対局もID基準で検出できるようにした。
- 検証: 関連Vitest 70/70 passed、`npm run test` 382/382 passed、`npm run build` 成功、`/home/mimura/.deno/bin/deno check --node-modules-dir=auto supabase/functions/manage_game_action/index.ts` 成功、Deno shared tests 5 suites passed。Supabase `manage_game_action` を本番デプロイし、GETで `local-20260718171351` を確認。Vercel production deploy `dpl_2ThnrzybXh8K8JktjwKtYUSKLx8L` 完了、`https://online.mimura15.jp` 配信JSに `list_active_for_players` / `講師一覧に出ていない対局` / `状態解除` が含まれることを確認。
- 2026-07-18: 講師の2局同時対局で、`sid:<id>` と素の `<id>` の表記差により片方の生徒が自分の対局として認識できず、碁盤を開けない/着手できない可能性を修正。`identityMatchesPlayer` / `studentIdentityCandidates` を追加し、生徒ロビー、講師生徒表、講師サムネイル、多面打ち手番判定、旧LiveKit経路の対局者判定を統一。講師を含む複数対局の一括作成時も講師対局ウィンドウを開くようにした。あわせて置き石定義の座標コメント/実座標の逆転を修正。
- 検証: 関連Vitest 81/81 passed、`npm run test` 375/375 passed、`npm run build` 成功、`/home/mimura/.deno/bin/deno test --allow-env --allow-net supabase/functions/_shared/sgf.test.ts` 成功。
- 2026-07-18: 講師が教室内の生徒一覧から、生徒ID/ログインコードを含む全生徒情報（姓名、棋力、内部R、種別、生年月日、学年、所在地）を編集できるように修正。生徒ID変更時は旧ID行の教室所属と表示順を新IDへ引き継ぎ、既存IDとの重複は保存前に拒否する。
- 検証: `npm run test -- src/utils/classroomStore.test.ts` 6/6 passed、`npm run build` 成功。
- 2026-07-17: 置き石配置を三村囲碁オンライン仕様に変更。2子は右上・左下、3子は左上を空けて右下を追加する順序に統一し、ローカル対局生成・通常SGF出力・Supabase Edge側SGF出力のテストを追加。
- 検証: 対象Vitest 36/36 passed、`/home/mimura/.deno/bin/deno test --allow-env --allow-net supabase/functions/_shared/sgf.test.ts` 成功、`npm run build` 成功。`npm run test` は 365/366 passed、`AutoPairingDialog` の1件が全体並列実行でタイムアウトしたが、単体再実行では4/4 passed。
- 2026-07-07: Live対局の終局時に保存棋譜テーブルへSGFが作られず、生徒別履歴検索も `sid:<uuid>` 形式を見ていなかったため、終局後の棋譜履歴に出ない問題を修正。
- 検証: `npm run test` 309/309 passed、`npm run build` 成功、`deno test --allow-env --allow-net supabase/functions/_shared/sgf.test.ts supabase/functions/_shared/identity.test.ts` 成功。
- 2026-07-07: 対局・検討/棋譜・問題を碁盤フォーカスの全画面表示に統一し、全画面中はホーム画面のヘッダー/参加者/音声パネルを隠すように修正。各画面に「閉じてホーム」ボタンを追加。
- 検証: `npm run test` 309/309 passed、`npm run build` 成功。
